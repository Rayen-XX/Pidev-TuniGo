<form method="post" action="{{ path('app_bus_trajet_delete', {'id': bus_trajet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bus_trajet.id) }}">
    <button class="btn">Delete</button>
</form>
