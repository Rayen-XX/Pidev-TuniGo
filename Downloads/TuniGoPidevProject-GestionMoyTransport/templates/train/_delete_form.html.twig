<form method="post" action="{{ path('app_train_delete', {'idTrain': train.idTrain}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ train.idTrain) }}">
    <button class="btn">Delete</button>
</form>
