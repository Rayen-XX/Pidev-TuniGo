<form method="post" action="{{ path('app_trajetbus_delete', {'idTrajetBus': trajetbu.idTrajetBus}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trajetbu.idTrajetBus) }}">
    <button class="btn">Delete</button>
</form>
