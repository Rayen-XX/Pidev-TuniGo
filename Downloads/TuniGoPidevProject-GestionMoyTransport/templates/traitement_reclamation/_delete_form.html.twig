<form method="post" action="{{ path('app_traitement_reclamation_delete', {'id': traitement_reclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traitement_reclamation.id) }}">
    <button class="btn">Delete</button>
</form>
