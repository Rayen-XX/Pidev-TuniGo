{% extends 'base.html.twig' %}

{% block title %}Bus{% endblock %}

{% block body %}
    <h1>Bus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdBus</th>
                <td>{{ bus.idBus }}</td>
            </tr>
            <tr>
                <th>NumeroBus</th>
                <td>{{ bus.numeroBus }}</td>
            </tr>
            <tr>
                <th>IdTrajetBus</th>
                <td>{{ bus.idTrajetBus }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bus_index') }}">back to list</a>

    <a href="{{ path('app_bus_edit', {'idBus': bus.idBus}) }}">edit</a>

    {{ include('bus/_delete_form.html.twig') }}
{% endblock %}
