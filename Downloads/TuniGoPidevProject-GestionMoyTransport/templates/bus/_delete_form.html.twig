<form method="post" action="{{ path('app_bus_delete', {'idBus': bus.idBus}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bus.idBus) }}">
    <button class="btn">Delete</button>
</form>
